mutation UpdateArticle($inputs: ArticleInputs!, $id: Int!) {
  updateArticle(inputs: $inputs, id:$id) {
    article {
      ...ArticleFragment 
    }
    errors {
      ...RegularFieldErrors
    }
  }
}
